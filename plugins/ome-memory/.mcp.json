{
  "mcpServers": {
    "memory": {
      "command": "uvx",
      "args": ["ome-memory-mcp"],
      "env": {
        "DATABASE_URL": "postgresql://memory:memory@localhost:5488/memory"
      }
    }
  }
}
